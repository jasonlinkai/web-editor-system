import { styled } from "@mui/material";
import MUIToggleButton from "@mui/material/ToggleButton";

const Button = styled(MUIToggleButton)({
  padding: "3px 6px",
  minWidth: "24px !important",
});

export default Button;
